{
  "id": "thalia",
  "available": false,
  "name": "Thalia",
  "description": "Fetches metadata from Thalia's public search API - a major book retailer in Germany.",
  "url": "https://www.thalia.de/",
  "parameters": [
    {
      "name": "limit",
      "required": false,
      "validation": {
        "type": "int",
        "min": 1,
        "max": 30
      },
      "description": "Maximum number of results to return (default: 3, max: 30)"
    },
    {
      "name": "type",
      "required": false,
      "validation": {
        "type": "string",
        "enum": ["ebook", "audiobook", "all"]
      },
      "description": "Type of the book to search for (default: all)"
    }
  ],
  "returnedFields": [
    "title",
    "subtitle",
    "author",
    "narrator",
    "description",
    "cover",
    "isbn",
    "series",
    "language",
    "publishedYear",
    "publisher",
    "duration",
    "tags"
  ],
  "comments": ["Currently does not work because of CF"]
}
